# Appendix

## Source Consistency Notes

- Halo Energy split labels differ between the overview and the dedicated analysis slide; both select 20 keV.
- A presentation-code header shows a Halo tilt default different from the final selected 25° condition; the dedicated code PDF and final-condition slide are treated as the final-condition evidence.
- Parameter defaults in a script header are not assumed to equal the final Workbench split selection.

## Reproducibility

The source files in `source/` are transcribed from the submitted code PDF. They have not been re-run as part of this GitHub portfolio reconstruction.
